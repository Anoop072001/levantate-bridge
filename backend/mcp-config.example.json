{
  "mcpServers": {
    "levantate-bridge": {
      "command": "npm",
      "args": ["run", "mcp", "--prefix", "/absolute/path/to/levantate-bridge/backend"],
      "env": {
        "LEVANTATE_AGENT_API_KEY": "lb_paste_key_from_register"
      }
    }
  }
}
